# HPE サーバー依存 (ONNX ベース、PyTorch 不使用)
#
# Windows 配布は python_embed/ で自己完結しているため、通常は直接使用しない。
# macOS 開発時に .venv で pip install -r するためのリファレンス。

# rtmlib (RTMPose / YOLO backend) - ローカルコピーから:
#   .venv/Scripts/pip install ./rtmlib/ --no-deps   (Windows 開発)
#   .venv/bin/pip install ./rtmlib/ --no-deps       (macOS)
rtmlib>=0.0.15

fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
numpy>=1.24.0,<2.0.0
opencv-python>=4.8.0

# Windows GPU 配布 (HPE-GPU) は build_python_embed.ps1 -Mode Gpu で
# onnxruntime-gpu[cuda,cudnn] を入れる。CUDA/cuDNN DLL は python_embed 直下に同梱。
# Windows CPU 配布 (HPE-CPU) は build_python_embed.ps1 -Mode Cpu で
# onnxruntime のみ。
onnxruntime-gpu>=1.20.0,<1.25.0; sys_platform == "win32"
# macOS: Apple Silicon は CoreML 対応の onnxruntime 純正版を使用
onnxruntime>=1.20.0; sys_platform != "win32"
onnx

Pillow>=10.0.0
tqdm>=4.66.0
scipy>=1.11.0

filterpy>=1.4.0
ffmpeg-python>=0.2.0
norfair>=2.2.0
